<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:id="@+id/login_llCircleProgressBar"
              android:layout_width="wrap_content"
              android:layout_height="wrap_content"
              android:padding="20dp"
              android:gravity="center_vertical|center_horizontal"
              android:background="@drawable/round_borders_dialog"
              android:orientation="vertical">
    <ProgressBar
            android:id="@+id/mainSpinner1"
            style="?android:attr/progressBarStyleInverse"
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:layout_gravity="center"
            android:layout_centerInParent="true"
            android:indeterminate="true"></ProgressBar>

    <TextView
            android:id="@+id/txtDescripcionLoading"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:textSize="14sp"
            android:fontFamily="@string/font_family_light"
            android:gravity="center_horizontal"
            android:text="Enviando"
            android:textColor="@color/color_control_highlight"></TextView>

</LinearLayout>